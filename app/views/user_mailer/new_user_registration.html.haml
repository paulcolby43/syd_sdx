!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
  %body
    %h2= "New User Sign Up"
    %p
      Name:
      - if not (@user.first_name.blank? and @user.last_name.blank?)
        = "#{@user.first_name} #{@user.last_name}"
      - else
        = @user.username
    %p
      Email:
      = "#{@user.email}"
    %p
      Company:
      = "#{@user.company_name}"
    - unless @user.phone.blank?
      %p
        Phone:
        = "#{number_to_phone(@user.phone)}" unless @user.phone.blank?
    %p
      Address:
      %br
      = @user.address1
      - unless @user.address2.blank?
        = @user.address2
      %br
      = "#{@user.city} #{@user.state} #{@user.zip}"
    - unless @user.dragon_account_number.blank?
      %p 
        Dragon Account Number:
        = @user.dragon_account_number
      